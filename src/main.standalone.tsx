/**
 * Entrada do build autocontido.
 *
 * Difere da entrada padrão em um único ponto: a tipografia da marca é
 * embutida no próprio bundle, já que a página final pode rodar sem acesso
 * a um CDN de fontes.
 */
import './styles/fonts.embedded.css';
import './main';
