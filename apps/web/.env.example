# Endereço da API. Em produção o Docker injeta este valor no build.
VITE_API_URL=http://localhost:3333/v1

# `mock` usa o banco de demonstração em memória; `api` fala com a API real.
VITE_DATA_SOURCE=mock
