{
  "name": "@myhome/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Domínio, matriz de permissões e contratos de API compartilhados entre o aplicativo e a API",
  "type": "module",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.esm.json && tsc -p tsconfig.cjs.json && node scripts/stamp-module-type.mjs",
    "dev": "tsc -p tsconfig.esm.json --watch --preserveWatchOutput",
    "lint": "tsc -p tsconfig.esm.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
