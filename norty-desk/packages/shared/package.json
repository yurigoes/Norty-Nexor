{
  "name": "@norty-desk/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Domínio, matriz de permissões e contratos de API do Norty Desk",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
